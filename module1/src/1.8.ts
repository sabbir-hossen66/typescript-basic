{
  // Interface e  { er moddhe sob type likhe hoy }

  // inter face e kono property add korte cayle extends name { er moddhe likhte hobe}

  // type alias e - type name =  evabe  type likhte hoy

  // type alias e barate hole type newName = ager kichu & { property: string }

  type Roll = number[];

  interface User {
    name: string;
    age: number;
  }

  const roll: Roll = [2, 3, 5, 76]

  const user: User = {
    name: 'sabbir',
    age: 23,
  }

}